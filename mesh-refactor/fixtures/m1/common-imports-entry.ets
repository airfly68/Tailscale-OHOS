// Deliberately invalid sample: the M1 static suite must reject this direction.
import { EntryAbility } from '../../../../entry/src/main/ets/entryability/EntryAbility';

export const invalidEntryDependency = EntryAbility;
