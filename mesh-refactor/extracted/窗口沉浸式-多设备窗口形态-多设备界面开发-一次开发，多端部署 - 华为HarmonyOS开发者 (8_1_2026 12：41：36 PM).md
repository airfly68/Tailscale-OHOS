# 窗口沉浸式-多设备窗口形态-多设备界面开发-一次开发，多端部署 - 华为HarmonyOS开发者

## 概述

### 概述

沉浸式模式是指通过减少无关元素的干扰，使应用界面更加专注于内容呈现，以提升用户体验的设计模式。典型应用的全屏窗口，其UI元素包括 、应用界面和底部 。其中，状态栏和导航条在沉浸式布局下称为避让区，避让区之外的区域称为 （如下图）。沉浸式页面开发通常通过将应用页面延伸至状态栏和导航条，达到以下目的： 状态栏 导航条 安全区

### 概述

- 视觉统一：应用页面与 （状态栏、导航条）色调统一，避免界面割裂，提供更好的视觉体验。 避让区 布局扩展：充分利用屏幕可视区域，使页面内容延伸到状态栏和导航条区域（即“安全区”之外的避让区），获得更大的布局空间。 沉浸体验：在游戏、视频等场景中隐藏系统元素，提供无干扰的全屏体验。

### 概述

本文将介绍沉浸式原理、实现方案，并提供常见沉浸式页面开发场景下适配问题的解决方案。

### 概述

- 顶部或底部背景延伸案例 挖孔避让案例 自由窗口标题栏沉浸案例

## 实现原理

### 概述 > 实现原理

沉浸式实现主要考虑如下两个因素：

### 概述 > 实现原理

- 实现沉浸式效果：通过特定方法、属性或接口，使页面突破安全区或标题栏限制，延伸至目标区域。 页面沉浸式效果实现方案分为窗口、组件两个层级： 窗口级：应用级全局沉浸，作用于所有页面。 组件级：作用于当前组件，可针对单个页面内的多个组件分别配置。 避让处理：避免页面内容与 的系统信息（如电量、时间）、交互功能（如导航条手势）或窗口控制键（如关闭、最小化）发生遮挡和冲突。 避让区

## 实现沉浸式效果

### 概述 > 实现原理 > 实现沉浸式效果

- 方案一：组件设置背景沉浸（组件级） 组件与避让区边界重合时，设置组件的 属性，将组件背景扩展至避让区，页面布局仍在安全区内。 background() 该接口为组件级，不同组件可设置不同背景色并扩展至对应避让区（如顶部组件设置红色背景并扩展至顶部状态栏，底部组件设置蓝色背景并扩展至底部导航条）。若页面背景色统一，可设置最外层父组件的background()属性，实现页面的沉浸式效果。 此方案中，只需设置一次代码属性，即可实现同一组件在不同窗口模式、窗口方向下的沉浸效果。但界面元素仅做绘制延伸，无法单独布局至状态栏和导航条区域。当页面支持滚动时，滚动区域无法延伸至状态栏和导航条。 原始布局（设置backgroundColor()效果） 组件设置背景沉浸 说明 ：背景颜色限定在安全区内。 backgroundColor() ：背景颜色扩展至顶部状态栏及底部导航条区域。 background() 方案二：组件设置页面沉浸（组件级） 通过设置 并设置高度为LayoutPolicy.matchParent适应父组件，页面背景与布局均扩展至顶部状态栏和底部导航条。作为组件级的沉浸式方案，每个页面均需单独配置。当页面内容与避让区发生冲突时，需由开发者手动进行 处理。 ignoreLayoutSafeArea() 避让 原始布局 组件设置页面沉浸 方案三：安全区域拓展（组件级） 设置组件的 属性，将组件的安全区域延伸至状态栏或导航条区域，同时保持子组件在安全区内布局，无需额外避让处理。支持指定系统避让区域类型（ ）和延伸方向（ ），实现沉浸式的方案可参考 。 expandSafeArea SafeAreaType SafeAreaEdge 组件安全区方案 原始布局 扩展至状态栏和导航条 仅扩展至状态栏 仅扩展至导航条 说明 边界重合要求：组件必须与安全区域边界重合（例如顶部组件需紧贴屏幕顶部）。 组件尺寸限制：配置expandSafeArea属性将组件进行绘制扩展时，需要关注组件不能配置固定宽高尺寸，百分比除外。 滚动容器限制：当父容器是滚动容器时，expandSafeArea属性设置无效。 方案四：窗口设置沉浸式显示（窗口级） 调用窗口强制全屏布局接口 设置窗口为沉浸式布局。页面布局范围从安全区域扩展为整个窗口（包括状态栏和导航条），布局范围扩展效果同 。 setWindowLayoutFullScreen() 组件设置页面沉浸 该方案为窗口级沉浸方案，沉浸式布局生效时，所有页面均开启全屏模式，需要开发者在各页面进行避让处理。

### 概述 > 实现原理 > 实现沉浸式效果

各沉浸式方案对比如下：

### 概述 > 实现原理 > 实现沉浸式效果

对比维度 | 组件设置背景沉浸 | 组件设置页面沉浸 | 安全区域拓展 | 窗口设置沉浸式显示
级别 | 组件级 | 组件级 | 组件级 | 窗口级
特点 | 仅背景扩展至避让区，内容默认在安全区 | 背景与内容均扩展至避让区 | 仅背景扩展至避让区，内容默认在安全区 支持指定系统避让区域类型和延伸方向 | 所有页面布局范围扩展至全屏，不自动避让
使用场景 | 仅页面背景沉浸的场景 | 需组件内容完全覆盖屏幕的场景 | 需要指定延伸区域类型的局部延伸场景 | 应用所有页面均需实现沉浸式
优点 | 使用简单，易理解 一次配置适配不同窗口模式、窗口方向 支持多组件不同背景扩展 | 使用简单，易理解 一次配置适配不同窗口模式、窗口方向 | 能够对扩展不同的安全区域进行配置 子组件仍在安全区内布局，无需额外的避让处理 | 全局统一生效，适配逻辑集中
缺点 | 滚动内容无法延伸至避让区 需为每个页面单独配置 | 内容易与避让区元素冲突，需手动处理避让 需为每个页面单独配置 | 滚动内容无法延伸至避让区 参数复杂，理解成本过高 | 需手动计算避让区高度，处理所有内容避让

### 概述 > 实现原理 > 实现沉浸式效果

窗口沉浸式效果由不同页面的沉浸实现，页面沉浸式效果由不同组件的沉浸策略组合而成。建议通过组件沉浸方案为每个页面配置不同的沉浸策略，以实现应用的沉浸效果。

## 避让处理

### 概述 > 实现原理 > 避让处理

由于避让区本身有内容展示（如状态栏中的电量、时间等系统信息），或是手势交互（如导航条点击或上滑），在实现应用页面沉浸式效果后，往往会和避让区域产生UI元素的遮挡、视觉上的违和或交互上的冲突等问题，开发者可以针对不同场景选择合适的方式进行适配。

### 概述 > 实现原理 > 避让处理

- 使用 方法或 方法设置状态栏和导航条的显隐。 Window.setWindowSystemBarEnable() Window.setSpecificSystemBarEnabled() 显示状态栏和导航条 隐藏状态栏和导航条 使用 方法获取避让区域的高度，设置应用页面内容的上下padding实现避让状态栏和导航条。 Window.getWindowAvoidArea() 避让前 避让后 使用 动态监听避让区变化。 on('avoidAreaChange') 通过AvoidAreaType.TYPE_SYSTEM获取状态栏区域信息。 通过AvoidAreaType.TYPE_NAVIGATION_INDICATOR获取底部导航条区域信息。 通过AvoidAreaType.TYPE_CUTOUT获取挖孔区信息。

## 自由窗口标题栏沉浸

### 概述 > 实现原理 > 自由窗口标题栏沉浸

在PC设备及平板和Mate XTs设备的自由多窗模式下，开发者可以设置应用窗口的标题栏不可见，将应用页面扩展至原标题栏区域，实现窗口的沉浸式效果。组件沉浸式的策略不适用于自由窗口标题栏的沉浸，需要开发者单独适配。

### 概述 > 实现原理 > 自由窗口标题栏沉浸

自由窗口标题栏包含应用图标、应用名称和三个按钮（全屏/还原、最小化、关闭）。通过设置标题栏不可见，隐藏应用图标和应用名称，保留三键区，实现窗口的沉浸式效果。

### 概述 > 实现原理 > 自由窗口标题栏沉浸

- 使用 设置隐藏标题栏，只显示右上角三键，此时应用页面拓展至标题栏区域。 setWindowDecorVisible(false) 使用 接口设置标题栏高度，控制右上角三键区显示以及显示高度。 setWindowDecorHeight() 使用 接口设置右上角按钮颜色、大小、间距、圆角半径等样式。 setDecorButtonStyle() 使用 获取窗口标题栏上的三键区位置和大小，用于不同场景下的页面布局避让。 getTitleButtonRect() 使用 注册右上角三键大小变化监听。 on('windowTitleButtonRectChange')

## 顶部或底部背景延伸案例

## 场景描述

### 概述 > 顶部或底部背景延伸案例 > 场景描述

页面背景需延伸至状态栏与导航条区域，页面内容在安全区内展示。

### 概述 > 顶部或底部背景延伸案例 > 场景描述

全屏 | 分屏 | 悬浮窗
| |

## 开发步骤

### 概述 > 顶部或底部背景延伸案例 > 开发步骤

- 方案一：组件设置背景沉浸 组件设置 实现背景延伸，内容自动保留在安全区内，自动适配不同窗口模式和窗口方向下避让区的变化。 background() 收起 自动换行 深色代码主题 复制 () { Column // ... } // Add a background for the component to achieve immersive background effect . ( ) background '#F1F3F5' Shopping.ets 方案二：组件设置页面沉浸 + 避让处理 通过设置 并设置高度为LayoutPolicy.matchParent适应父组件，将页面扩展至避让区。 ignoreLayoutSafeArea() 收起 自动换行 深色代码主题 复制 () { Column // ... } // Expands the layout safe area of a component. . () ignoreLayoutSafeArea . ( . ) height LayoutPolicy matchParent ShoppingAvoid.ets 通过 方法获取状态栏和导航条高度，并用状态变量avoidSystem记录。 Window.getWindowAvoidArea() 收起 自动换行 深色代码主题 复制 // Get the avoid area. . . = . . ( . . ); this mainWindowInfo avoidSystem this mainWindow getWindowAvoidArea window AvoidAreaType TYPE_SYSTEM WindowUtil.ets 使用 事件监听避让区域的变化，在监听回调中更新状态变量avoidSystem。 on('avoidAreaChange') 收起 自动换行 深色代码主题 复制 : = public onAvoidAreaChange ( ) => avoidOptions: .AvoidAreaOptions window void { ( ) => avoidOptions: .AvoidAreaOptions window (avoidOptions. === . . ) { if type window AvoidAreaType TYPE_SYSTEM // Default area of the system. . . = avoidOptions. ; this mainWindowInfo avoidSystem area } (avoidOptions. === . . ) { else if type window AvoidAreaType TYPE_CUTOUT // Cutout area. . . = avoidOptions. ; this mainWindowInfo avoidCutout area . (avoidOptions. ); this updateAvoidPadding area } (avoidOptions. === . . ) { else if type window AvoidAreaType TYPE_SYSTEM_GESTURE // Side return gesture area. . . = avoidOptions. ; this mainWindowInfo avoidSystemGesture area } (avoidOptions. === . . ) { else if type window AvoidAreaType TYPE_KEYBOARD // Fixed soft keyboard area. . . = avoidOptions. ; this mainWindowInfo avoidKeyboard area } (avoidOptions. === . . ) { else if type window AvoidAreaType TYPE_NAVIGATION_INDICATOR // Bottom navigation bar. . . = avoidOptions. ; this mainWindowInfo avoidNavigationIndicator area } }; WindowUtil.ets 设置组件的上下padding分别为状态栏和导航条的高度，使内容避让状态栏和导航条，仅在安全区域显示。 收起 自动换行 深色代码主题 复制 () { Column // ... } // ... // Set the top and bottom padding to avoid status bar and navigation bar. . ({ padding : . ?. . ?. . + , top this windowUtil mainWindowInfo avoidSystem topRect height 'px' : . ?. . ?. . + bottom this windowUtil mainWindowInfo avoidNavigationIndicator bottomRect height 'px' }) ShoppingAvoid.ets 推荐使用上述两种方案实现，相比窗口设置沉浸显示和安全区域拓展，这两种方案实现方式简单易理解，且自动适配不同窗口模式和方向。

## 案例 挖孔避让

## 场景描述

### 概述 > 案例 挖孔避让 > 场景描述

在需要充分利用屏幕可视区域的场景中，若要在状态栏显示重要信息或操作按钮，不仅需隐藏原始状态栏和导航条，还需对挖孔区域进行避让。这种情况下可以将背景全屏沉浸，内容区对挖孔区域做避让处理。

### 概述 > 案例 挖孔避让 > 场景描述

在以下场景中，挖孔区尺寸可能发生变化，因此需要监听避让区高度等信息的变化，确保UI布局及时适配调整。

### 概述 > 案例 挖孔避让 > 场景描述

- 窗口模式切换（全屏/悬浮窗/分屏等） 设备旋转 折叠状态切换（展开、折叠）

### 概述 > 案例 挖孔避让 > 场景描述

以设备旋转为例，效果图如下。

### 概述 > 案例 挖孔避让 > 场景描述

竖屏-挖孔区在顶部 | 横屏-挖孔区在左侧 | 反向竖屏-挖孔区在底部 | 反向横屏-挖孔区在右侧
| | |

## 开发步骤

### 概述 > 案例 挖孔避让 > 开发步骤

- 使用 和height(LayoutPolicy.matchParent)，实现页面沉浸。 ignoreLayoutSafeArea() 收起 自动换行 深色代码主题 复制 () { Column // ... } // Expands the layout safe area of a component. . () ignoreLayoutSafeArea . ( . ) height LayoutPolicy matchParent MiniGame.ets 使用 隐藏状态栏和导航条。 setWindowSystemBarEnable() 收起 自动换行 深色代码主题 复制 // Hide the status bar and navigation bar. . . ([]). ( { this mainWindow setWindowSystemBarEnable catch ( ) => error: BusinessError err = error ; let as BusinessError hilog. ( , , ); error 0x0000 'testLog' `setWindowSystemBarEnable failed. Code: , message: ` ${err.code} ${err.message} }); WindowUtil.ets 使用 动态监听避让区变化，通过AvoidAreaType.TYPE_CUTOUT来获取挖孔区域信息。 on('avoidAreaChange') 收起 自动换行 深色代码主题 复制 : = public onAvoidAreaChange ( ) => avoidOptions: .AvoidAreaOptions window void { ( ) => avoidOptions: .AvoidAreaOptions window (avoidOptions. === . . ) { if type window AvoidAreaType TYPE_SYSTEM // Default area of the system. . . = avoidOptions. ; this mainWindowInfo avoidSystem area } (avoidOptions. === . . ) { else if type window AvoidAreaType TYPE_CUTOUT // Cutout area. . . = avoidOptions. ; this mainWindowInfo avoidCutout area . (avoidOptions. ); this updateAvoidPadding area } (avoidOptions. === . . ) { else if type window AvoidAreaType TYPE_SYSTEM_GESTURE // Side return gesture area. . . = avoidOptions. ; this mainWindowInfo avoidSystemGesture area } (avoidOptions. === . . ) { else if type window AvoidAreaType TYPE_KEYBOARD // Fixed soft keyboard area. . . = avoidOptions. ; this mainWindowInfo avoidKeyboard area } (avoidOptions. === . . ) { else if type window AvoidAreaType TYPE_NAVIGATION_INDICATOR // Bottom navigation bar. . . = avoidOptions. ; this mainWindowInfo avoidNavigationIndicator area } }; WindowUtil.ets 根据挖孔区方位，确定避让方向，并记录在状态变量avoidAreaPadding中。 挖孔区在顶部，则顶部避让 = 挖孔区距屏幕顶部边缘的距离 + 挖孔区域的高度。 挖孔区在左侧，则左侧避让 = 挖孔区距屏幕左侧边缘的距离 + 挖孔区域的宽度。 挖孔区在右侧，则右侧避让 = 屏幕宽度 - 挖孔区距屏幕左侧边缘的距离。 挖孔区在底部，则底部避让 = 屏幕高度 - 挖孔区距屏幕顶部距离。 收起 自动换行 深色代码主题 复制 ( ) { updateAvoidPadding area: .AvoidArea window : = {}; const avoidAreaPadding Padding { try // Top cutout area: set top padding. (area. . > ) { if topRect height 0 avoidAreaPadding. = . ?. (area. . + area. . ); top this uiContext px2vp topRect height topRect top } // Left cutout area: set left padding. (area. . > ) { if leftRect width 0 avoidAreaPadding. = . ?. (area. . + area. . ); left this uiContext px2vp leftRect left leftRect width } // Right cutout area: set right padding. (area. . > ) { if rightRect width 0 avoidAreaPadding. = . ?. (display. (). - area. . ); right this uiContext px2vp getDefaultDisplaySync width rightRect left } // Bottom cutout area: set bottom padding. (area. . > ) { if bottomRect height 0 avoidAreaPadding. = . ?. (display. (). - area. . ); bottom this uiContext px2vp getDefaultDisplaySync height bottomRect top } } (error) { catch err = error ; let as BusinessError hilog. ( , , ); error 0x0000 'TestLog' `Failed to set avoid padding. Code: , message: ` ${err.code} ${err.message} } . . = avoidAreaPadding; this mainWindowInfo avoidAreaPadding } WindowUtil.ets 设置组件padding，进行避让。 收起 自动换行 深色代码主题 复制 () { Column // ... } // ... // Set paddings to avoid notch. . ( . ?. ) padding this mainWindowInfo avoidAreaPadding MiniGame.ets

## 案例 自由窗口标题栏沉浸

## 场景描述

### 概述 > 案例 自由窗口标题栏沉浸 > 场景描述

自由窗口模式下，需隐藏默认标题栏，保留关闭、最小化、最大化三键区，使页面延伸至原标题栏区域，实现窗口沉浸式。

## 开发步骤

### 概述 > 案例 自由窗口标题栏沉浸 > 开发步骤

- 使用 接口设置标题栏的隐藏，仅保留右上角的三键区。 setWindowDecorVisible(false) 收起 自动换行 深色代码主题 复制 . . ( ); this mainWindow setWindowDecorVisible false WindowUtil.ets 使用 接口设置标题栏高度，控制右上角三键区显示高度。 setWindowDecorHeight() 收起 自动换行 深色代码主题 复制 . . ( ); this mainWindow setWindowDecorHeight 56 WindowUtil.ets 使用 接口设置右上角按钮大小，和页面顶部icon大小保持一致。 setDecorButtonStyle() 收起 自动换行 深色代码主题 复制 . . ({ : }); this mainWindow setDecorButtonStyle buttonIconSize 24 WindowUtil.ets 使用 注册右上角三键大小变化监听，用于页面布局避让。 on('windowTitleButtonRectChange') 收起 自动换行 深色代码主题 复制 : = public onWindowButtonRectChange ( ) => titleButtonRect: .TitleButtonRect window void { ( ) => titleButtonRect: .TitleButtonRect window // Update the rectangle information of the title bar button. . . = titleButtonRect; this mainWindowInfo titleButtonRect } WindowUtil.ets 使用padding进行布局避让。 收起 自动换行 深色代码主题 复制 () { Column // ... } . ({ padding : . . === . ? . . ?. : right this mainWindowInfo isImmersive ImmersiveType IMMERSIVE this mainWindowInfo titleButtonRect width 0 }) Header.ets

## 示例代码

### 概述 > 示例代码

沉浸式页面开发

### 概述 > 示例代码

如果您有其他疑问，您也可以通过开发者社区问答频道来和我们联系探讨。

### 概述 > 实现原理 > 实现沉浸式效果

SafeAreaType

### 概述 > 实现原理 > 实现沉浸式效果

SafeAreaEdge

### 概述 > 实现原理 > 避让处理

Window

### 概述 > 顶部或底部背景延伸案例 > 开发步骤

Column

### 概述 > 顶部或底部背景延伸案例 > 开发步骤

Add

### 概述 > 顶部或底部背景延伸案例 > 开发步骤

F1F3F5

### 概述 > 顶部或底部背景延伸案例 > 开发步骤

Shopping

### 概述 > 顶部或底部背景延伸案例 > 开发步骤

Expands

### 概述 > 顶部或底部背景延伸案例 > 开发步骤

LayoutPolicy

### 概述 > 顶部或底部背景延伸案例 > 开发步骤

ShoppingAvoid

### 概述 > 顶部或底部背景延伸案例 > 开发步骤

Get

### 概述 > 顶部或底部背景延伸案例 > 开发步骤

AvoidAreaType

### 概述 > 顶部或底部背景延伸案例 > 开发步骤

TYPE_SYSTEM

### 概述 > 顶部或底部背景延伸案例 > 开发步骤

WindowUtil

### 概述 > 顶部或底部背景延伸案例 > 开发步骤

AvoidAreaOptions

### 概述 > 顶部或底部背景延伸案例 > 开发步骤

Default

### 概述 > 顶部或底部背景延伸案例 > 开发步骤

TYPE_CUTOUT

### 概述 > 顶部或底部背景延伸案例 > 开发步骤

Cutout

### 概述 > 顶部或底部背景延伸案例 > 开发步骤

TYPE_SYSTEM_GESTURE

### 概述 > 顶部或底部背景延伸案例 > 开发步骤

Side

### 概述 > 顶部或底部背景延伸案例 > 开发步骤

TYPE_KEYBOARD

### 概述 > 顶部或底部背景延伸案例 > 开发步骤

Fixed

### 概述 > 顶部或底部背景延伸案例 > 开发步骤

TYPE_NAVIGATION_INDICATOR

### 概述 > 顶部或底部背景延伸案例 > 开发步骤

Bottom

### 概述 > 顶部或底部背景延伸案例 > 开发步骤

Set

### 概述 > 案例 挖孔避让 > 开发步骤

MiniGame

### 概述 > 案例 挖孔避让 > 开发步骤

Hide

### 概述 > 案例 挖孔避让 > 开发步骤

BusinessError

### 概述 > 案例 挖孔避让 > 开发步骤

Code

### 概述 > 案例 挖孔避让 > 开发步骤

err.code

### 概述 > 案例 挖孔避让 > 开发步骤

err.message

### 概述 > 案例 挖孔避让 > 开发步骤

AvoidArea

### 概述 > 案例 挖孔避让 > 开发步骤

Padding

### 概述 > 案例 挖孔避让 > 开发步骤

Top

### 概述 > 案例 挖孔避让 > 开发步骤

Left

### 概述 > 案例 挖孔避让 > 开发步骤

Right

### 概述 > 案例 挖孔避让 > 开发步骤

TestLog

### 概述 > 案例 挖孔避让 > 开发步骤

Failed

### 概述 > 案例 自由窗口标题栏沉浸 > 开发步骤

TitleButtonRect

### 概述 > 案例 自由窗口标题栏沉浸 > 开发步骤

Update

### 概述 > 案例 自由窗口标题栏沉浸 > 开发步骤

ImmersiveType

### 概述 > 案例 自由窗口标题栏沉浸 > 开发步骤

IMMERSIVE

### 概述 > 案例 自由窗口标题栏沉浸 > 开发步骤

Header
